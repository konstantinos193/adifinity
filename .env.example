# Email Configuration (for contact form)
# Sent through our own Stalwart SMTP server (mail.adinfinity.gr) via Nodemailer.
# Recipients are fixed in app/api/contact/route.ts (manos@ + team in CC).
EMAIL_HOST=mail.adinfinity.gr
EMAIL_PORT=465
EMAIL_USER=contact@adinfinity.gr
EMAIL_PASS=your-smtp-password
